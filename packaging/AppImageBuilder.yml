version: __VERSION__

AppDir:
  path: ./AppDir
  app_info:
    id: com.orbproject.OrbProject
    name: OrbProject
    icon: OrbProject
    exec: usr/bin/OrbProject
    version: __VERSION__
  runtime:
    env:
      APPDIR_LIBRARY_PATH: '$APPDIR/usr/lib'
