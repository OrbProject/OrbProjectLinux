name: Package and Release Linux Builds

on:
  release:
    types: [created]  # Déclenche l'action uniquement lors de la création d'une release

permissions:
  contents: write


jobs:
  package:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Récupérer le numéro de version depuis la release
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

      - name: Afficher la structure des fichiers
        run: ls -R

      - name: Préparer .deb (Debian/Ubuntu)
        run: |
          mkdir -p debian/DEBIAN
          mkdir -p debian/usr/lib/orbproject
          mkdir -p debian/usr/bin

          cp -r OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* debian/usr/lib/orbproject/
          ln -s /usr/lib/orbproject/OrbProject.x86_64 debian/usr/bin/orbproject

          echo "Package: orbproject
          Version: $VERSION
          Section: games
          Priority: optional
          Architecture: amd64
          Depends: libc6
          Maintainer: TonNom <ton.email@example.com>
          Description: Un jeu fait avec Unity" > debian/DEBIAN/control

          dpkg-deb --build debian OrbProject_${VERSION}_amd64.deb

      - name: Créer les dossiers et copier les fichiers
        run: |
          mkdir -p $PWD/rpm/usr/lib/orbproject
          mkdir -p $PWD/rpm/usr/bin
      
          # Vérifie que les fichiers existent avant de les copier
          ls -l OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* || exit 1
      
          cp -r OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* $PWD/rpm/usr/lib/orbproject/
          ln -s /usr/lib/orbproject/OrbProject.x86_64 $PWD/rpm/usr/bin/orbproject
      
      - name: Vérifier le contenu des répertoires
        run: |
          echo "📁 Contenu de /home/runner/rpmbuild/BUILD/rpm avant copie :"
          mkdir -p /home/runner/rpmbuild/BUILD/rpm/usr/lib/orbproject  # Création du dossier ici !
          ls -R /home/runner/rpmbuild/BUILD/rpm || echo "❌ Le répertoire rpm n'existe pas"
      
          echo "📁 Contenu du dossier source (où se trouvent les fichiers à copier) :"
          ls -l $PWD/rpm/usr/lib/orbproject || echo "❌ Le dossier source est vide ou inexistant"
      
      - name: Générer le fichier spec et construire le RPM
        run: |
          cat <<EOF > orbproject.spec
          Name: orbproject
          Version: ${VERSION}
          Release: 1
          Summary: Un jeu fait avec Unity
          License: Proprietary
          Group: Games
          BuildArch: x86_64
          Requires: libc.so.6

          %description
          Un jeu Unity packagé pour Fedora.

          %install
          mkdir -p %{buildroot}/usr/lib/orbproject
          mkdir -p %{buildroot}/usr/bin
          cp -r rpm/usr/lib/orbproject %{buildroot}/usr/lib/ || echo "⚠️ Problème de copie !"
          ln -s /usr/lib/orbproject/OrbProject.x86_64 %{buildroot}/usr/bin/orbproject

          %files
          /usr/lib/orbproject
          /usr/bin/orbproject

          %post
          echo 'Installation terminée!'
          EOF

          rpmbuild -bb orbproject.spec --buildroot=$PWD/rpm
      - name: Vérifier le fichier RPM généré
        run: |
          echo "📁 Liste des fichiers RPM générés :"
          find ~/rpmbuild/RPMS/ -name "*.rpm" || echo "❌ AUCUN fichier RPM trouvé !"

      - name: Copier le RPM dans le dossier de build
        run: |
          mkdir -p artifacts
          cp $HOME/rpmbuild/RPMS/x86_64/*.rpm artifacts/
          
        shell: bash
        env:
          VERSION: ${{ env.VERSION }}

      - name: Sauvegarder le fichier RPM
        uses: actions/upload-artifact@v3
        with:
          name: rpm-package
          path: artifacts/*.rpm

      - name: Installer les dépendances Flatpak
        run: |
          sudo apt update && sudo apt install -y flatpak flatpak-builder
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub org.freedesktop.Platform//21.08
          sudo flatpak install -y flathub org.freedesktop.Sdk//21.08

      - name: Vérifier installation de Flatpak et des SDK
        run: |
          flatpak --version
          flatpak list

      - name: Générer le fichier de configuration Flatpak
        run: |
          cat <<EOF > orbproject.flatpak.json
          {
            "app-id": "com.orbproject.OrbProject",
            "runtime": "org.freedesktop.Platform",
            "runtime-version": "21.08",
            "sdk": "org.freedesktop.Sdk",
            "command": "/app/bin/orbproject",
            "modules": [
              {
                "name": "orbproject",
                "buildsystem": "simple",
                "build-commands": [
                  "install -D -m 755 bin/orbproject /app/bin/orbproject"
                ],
                "sources": [
                  {
                    "type": "dir",
                    "path": "flatpak/app"
                  }
                ]
              }
            ]
          }
          EOF

      - name: Préparer les fichiers pour Flatpak
        run: |
          mkdir -p flatpak/app/lib/orbproject
          mkdir -p flatpak/app/bin
          
          cp -vr OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* flatpak/app/lib/orbproject/
          cp flatpak/app/lib/orbproject/OrbProject.x86_64 flatpak/app/bin/orbproject

          chmod +x flatpak/app/bin/orbproject

          # Vérifier si l'exécutable est bien copié
          echo "📁 Vérification des fichiers après copie :"
          ls -l flatpak/app/bin/orbproject || exit 1

      - name: Construire le Flatpak
        run: |
          echo "📁 Contenu du dossier Flatpak avant la construction :"
          ls -lR flatpak/app/ || exit 1  # Vérifier la structure complète

          echo "📁 Chemin actuel :"
          pwd

          # Utiliser un chemin absolu pour éviter des problèmes de chemin relatif
          flatpak-builder --repo=repo build-dir orbproject.flatpak.json --force-clean
          flatpak build-bundle repo OrbProject_${VERSION}.flatpak com.orbproject.OrbProject

      - name: Upload Artifacts to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            OrbProject_${{ env.VERSION }}_amd64.deb
            artifacts/*.rpm
            OrbProject_${{ env.VERSION }}.flatpak
            
  publish-apt:
    runs-on: ubuntu-latest
    needs: package

    steps:
      - name: Récupérer le numéro de version depuis la release
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV
        
      - name: Installer Aptly
        run: |
          sudo apt update && sudo apt install -y aptly

      - name: Configurer le dépôt APT
        run: |
          mkdir -p aptly_repo
          cd aptly_repo
          aptly repo create -distribution=bullseye -component=main orbproject-repo

      - name: Ajouter le paquet au dépôt APT
        run: |
          cd aptly_repo
          aptly repo add orbproject-repo $GITHUB_WORKSPACE/OrbProject_${{ env.VERSION }}_amd64.deb
          aptly publish repo -batch orbproject-repo

      - name: Déployer le dépôt APT (GitHub Pages)
        run: |
          git clone --depth 1 https://github.com/OrbProject/OrbProject-apt.git aptly_repo
          cp -r aptly_repo/public/* OrbProject-apt/
          cd OrbProject-apt
          git add .
          git commit -m "Mise à jour du dépôt APT pour OrbProject v${{ env.VERSION }}"
          git push

  publish-yum:
    runs-on: ubuntu-latest
    needs: package

    steps:
      - name: Récupérer le numéro de version depuis la release
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV
        
      - name: Installer createrepo-c
        run: sudo apt update && sudo apt install -y createrepo-c

      - name: Télécharger le fichier RPM
        uses: actions/download-artifact@v3
        with:
          name: rpm-package
          path: artifacts

      - name: Configurer le dépôt YUM
        run: |
          mkdir -p yum_repo
          cp artifacts/*.rpm yum_repo/
          createrepo-c yum_repo/

      - name: Déployer le dépôt YUM (GitHub Pages)
        run: |
          git clone --depth 1 https://github.com/OrbProject/OrbProject-yum.git yum_repo_git
          cp -r yum_repo/* yum_repo_git/
          cd yum_repo_git
          git add .
          git commit -m "Mise à jour du dépôt YUM pour OrbProject v${{ env.VERSION }}"
          git push

  #publish-flatpak:
     #runs-on: ubuntu-latest
    #needs: package

#    steps:
    #  - name: Installer Flatpak et configurer Flathub
   #     run: |
        #  sudo apt update && sudo apt install -y flatpak flatpak-builder
       #   flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

     # - name: Déployer sur Flathub
      #  run: |
   #       flatpak build-bundle repo OrbProject_${{ env.VERSION }}.flatpak com.orbproject.OrbProject
         # flatpak build-import-bundle repo OrbProject_${{ env.VERSION }}.flatpak


