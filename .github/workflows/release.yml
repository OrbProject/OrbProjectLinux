name: Package and Release Linux Builds

on:
  release:
    types: [created]  # Déclenche l'action uniquement lors de la création d'une release

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Récupérer le numéro de version depuis la release
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

      - name: Afficher la structure des fichiers
        run: ls -R

      - name: Préparer .deb (Debian/Ubuntu)
        run: |
          mkdir -p debian/DEBIAN
          mkdir -p debian/usr/lib/orbproject
          mkdir -p debian/usr/bin

          cp -r OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* debian/usr/lib/orbproject/
          ln -s /usr/lib/orbproject/OrbProject.x86_64 debian/usr/bin/orbproject

          echo "Package: orbproject
          Version: $VERSION
          Section: games
          Priority: optional
          Architecture: amd64
          Depends: libc6
          Maintainer: TonNom <ton.email@example.com>
          Description: Un jeu fait avec Unity" > debian/DEBIAN/control

          dpkg-deb --build debian OrbProject_${VERSION}_amd64.deb

      - name: Créer les dossiers et copier les fichiers
        run: |
          mkdir -p $PWD/rpm/usr/lib/orbproject
          mkdir -p $PWD/rpm/usr/bin
      
          # Vérifie que les fichiers existent avant de les copier
          ls -l OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* || exit 1
      
          cp -r OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* $PWD/rpm/usr/lib/orbproject/
          ln -s /usr/lib/orbproject/OrbProject.x86_64 $PWD/rpm/usr/bin/orbproject
      
      - name: Vérifier le contenu des répertoires
        run: |
          echo "📁 Contenu de /home/runner/rpmbuild/BUILD/rpm avant copie :"
          mkdir -p /home/runner/rpmbuild/BUILD/rpm/usr/lib/orbproject  # Création du dossier ici !
          ls -R /home/runner/rpmbuild/BUILD/rpm || echo "❌ Le répertoire rpm n'existe pas"
      
          echo "📁 Contenu du dossier source (où se trouvent les fichiers à copier) :"
          ls -l $PWD/rpm/usr/lib/orbproject || echo "❌ Le dossier source est vide ou inexistant"
      
      - name: Générer le fichier spec et construire le RPM
        run: |
          cat <<EOF > orbproject.spec
          Name: orbproject
          Version: ${VERSION}
          Release: 1
          Summary: Un jeu fait avec Unity
          License: Proprietary
          Group: Games
          BuildArch: x86_64
          Requires: libc.so.6

          %description
          Un jeu Unity packagé pour Fedora.

          %install
          mkdir -p %{buildroot}/usr/lib/orbproject
          mkdir -p %{buildroot}/usr/bin
          cp -r rpm/usr/lib/orbproject %{buildroot}/usr/lib/ || echo "⚠️ Problème de copie !"
          ln -s /usr/lib/orbproject/OrbProject.x86_64 %{buildroot}/usr/bin/orbproject

          %files
          /usr/lib/orbproject
          /usr/bin/orbproject

          %post
          echo 'Installation terminée!'
          EOF

          rpmbuild -bb orbproject.spec --buildroot=$PWD/rpm
        shell: bash
        env:
          VERSION: ${{ env.VERSION }}

      - name: Installer les dépendances Flatpak
        run: |
          sudo apt update && sudo apt install -y flatpak flatpak-builder
          flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak install -y flathub org.freedesktop.Platform//22.08
          flatpak install -y flathub org.freedesktop.Sdk//22.08

          
      - name: Vérifier l'installation de Flatpak
        run: flatpak --version

      - name: Générer le fichier de configuration Flatpak
        run: |
          cat <<EOF > orbproject.flatpak.json
          {
            "app-id": "com.orbproject.OrbProject",
            "runtime": "org.freedesktop.Platform",
            "runtime-version": "22.08",
            "sdk": "org.freedesktop.Sdk",
            "command": "orbproject",
            "modules": [
              {
                "name": "orbproject",
                "buildsystem": "simple",
                "build-commands": [
                  "install -D -m 755 flatpak/app/bin/orbproject /app/bin/orbproject",
                  "cp -r flatpak/app/lib/orbproject /app/lib/"
                ],
                "sources": [
                  {
                    "type": "dir",
                    "path": "flatpak/app"
                  }
                ]
              }
            ]
          }
          EOF


      - name: Préparer Flatpak
        run: |
          mkdir -p flatpak/app/bin
          mkdir -p flatpak/app/lib/orbproject
          cp -r OrbProject.x86_64 OrbProject_Data UnityPlayer.so libdecor-*.so* flatpak/app/lib/orbproject/
          ln -s /app/lib/orbproject/OrbProject.x86_64 flatpak/app/bin/orbproject
          flatpak-builder --repo=repo build-dir orbproject.flatpak.json
          flatpak build-bundle repo OrbProject_${VERSION}.flatpak com.orbproject.OrbProject

      - name: Upload Artifacts to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            OrbProject_${VERSION}_amd64.deb
            OrbProject_${VERSION}_x86_64.rpm
            OrbProject_${VERSION}.flatpak
